# Astro
node_modules/
dist/
.astro/

# Use bun.lock — npm/yarn lockfiles would diverge from CI
package-lock.json
yarn.lock
pnpm-lock.yaml

# OS
.DS_Store

# Terraform
.terraform
.terraform.lock.hcl

# Agent config (AGENTS.md / .agents are the source of truth; CLAUDE.md /
# .claude are symlinks onto them, see AGENTS.md)
.agents/settings.local.json

# IDE
.vscode/
.idea/
